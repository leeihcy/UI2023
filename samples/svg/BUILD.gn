

executable("svg") {
    output_name = "sample_svg"

    deps = [
        "//ui/sdk:sdk",
        "//ui/svg:svg"
    ]
    include_dirs = [
        "//ui",
    ]
    sources = [
        "main.cc",
    ]

    cflags = []
    if (is_debug) {
      cflags += ["-g"]
    }
}