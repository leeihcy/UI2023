# import("//build/config/common.gni")

group("x") {
  deps = [
    "//ui:ui"
  ]
}

group("samples") {
  deps = [
    "//samples:ui"
  ]
}

if (is_debug) {
  group("unittest") {
    deps = [
      "//ui:unittest"
    ]
  }
}
 