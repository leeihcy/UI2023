# import("//build/config/common.gni")

group("ui") {
    deps = [
        "sdk:sdk",
        "gpu:gpu",
        "svg:uisvg",
    ]
}

######################################
if (is_debug) {

group("unittest") {
    deps = [
        "sdk:sdk_test",
        "gpu:gpu_test"
    ]
}

}
