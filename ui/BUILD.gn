# import("//build/config/common.gni")

group("ui") {
    deps = [
        "sdk:sdk",
        "gpu:gpu",
        "svg:uisvg",
    ]
}

######################################

group("unittest") {
    deps = [
        "sdk:sdk_test",
        "gpu:gpu_test"
    ]
}
